---
- hosts: coolify-master
  become: yes
  become_user: root
  tasks:
    - name: Allow incoming traffic on specified ports
      ufw:
        rule: allow
        port: "{{ item }}"
      loop:
        - 8000
        - 6001